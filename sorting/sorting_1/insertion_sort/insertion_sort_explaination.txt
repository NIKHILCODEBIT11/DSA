// =================================================================
        // STEP-BY-STEP EXECUTION TRACE IN COMMENTS
        // =================================================================

        // -----------------------------------------------------------------
        // i = 0 (Current element: arr[0] = 3)
        // -----------------------------------------------------------------
        // j = 0
        // while (j > 0 ...): Condition (0 > 0) is FALSE. Loop does not run.
        // State after i = 0: [3, 5, 2, 7, 1, 9]

        // -----------------------------------------------------------------
        // i = 1 (Current element: arr[1] = 5)
        // -----------------------------------------------------------------
        // j = 1
        // while (1 > 0 && 5 < 3): Condition (5 < 3) is FALSE. Loop does not run.
        // State after i = 1: [3, 5, 2, 7, 1, 9]

        // -----------------------------------------------------------------
        // i = 2 (Current element: arr[2] = 2)
        // -----------------------------------------------------------------
        // j = 2: (2 > 0 && 2 < 5) -> TRUE. swap(arr[2], arr[1]) -> [3, 2, 5, 7, 1, 9]. j becomes 1.
        // j = 1: (1 > 0 && 2 < 3) -> TRUE. swap(arr[1], arr[0]) -> [2, 3, 5, 7, 1, 9]. j becomes 0.
        // j = 0: (0 > 0) -> FALSE. Loop ends.
        // State after i = 2: [2, 3, 5, 7, 1, 9]

        // -----------------------------------------------------------------
        // i = 3 (Current element: arr[3] = 7)
        // -----------------------------------------------------------------
        // j = 3
        // while (3 > 0 && 7 < 5): Condition (7 < 5) is FALSE. Loop does not run.
        // State after i = 3: [2, 3, 5, 7, 1, 9]

        // -----------------------------------------------------------------
        // i = 4 (Current element: arr[4] = 1)
        // -----------------------------------------------------------------
        // j = 4: (4 > 0 && 1 < 7) -> TRUE. swap(arr[4], arr[3]) -> [2, 3, 5, 1, 7, 9]. j becomes 3.
        // j = 3: (3 > 0 && 1 < 5) -> TRUE. swap(arr[3], arr[2]) -> [2, 3, 1, 5, 7, 9]. j becomes 2.
        // j = 2: (2 > 0 && 1 < 3) -> TRUE. swap(arr[2], arr[1]) -> [2, 1, 3, 5, 7, 9]. j becomes 1.
        // j = 1: (1 > 0 && 1 < 2) -> TRUE. swap(arr[1], arr[0]) -> [1, 2, 3, 5, 7, 9]. j becomes 0.
        // j = 0: (0 > 0) -> FALSE. Loop ends.
        // State after i = 4: [1, 2, 3, 5, 7, 9]

        // -----------------------------------------------------------------
        // i = 5 (Current element: arr[5] = 9)
        // -----------------------------------------------------------------
        // j = 5
        // while (5 > 0 && 9 < 7): Condition (9 < 7) is FALSE. Loop does not run.
        // State after i = 5: [1, 2, 3, 5, 7, 9]